### Counter Project
#### A simple JavaScript counter that counts up or down between two numbers.
#### Users can enter the start and end values, and the counter will automatically adjust the direction.
#### If the start and end numbers are the same, an alert will notify the user: "Start and end numbers should not be equal!"
